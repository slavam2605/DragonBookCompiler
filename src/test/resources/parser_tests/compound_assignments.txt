fun test_main() {
    int a = 10
    int b = 3
    a += b
    a -= 1
    a *= 2
    a /= 3
    a %= 5

    float f = 1.5
    f += 2
    f -= 0.5
    f *= 2.0
    f /= 3.0
    // Note: modulo not allowed for float by semantics; not tested here
}
