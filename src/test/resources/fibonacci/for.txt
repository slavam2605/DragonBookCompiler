fun fibonacci(int n) -> int {
    int prev = 0
    int curr = 1
    for (int i = 0; i < n; i = i + 1) {
        int temp = curr
        curr = prev + curr
        prev = temp
    }
    return prev
}

fun test_main() -> int {
    return fibonacci(<$n>)
}