// memory_allocation test_main: used 3, spilled 0

fun test_main() {
    int i; i = 0;
    int sum; sum = 0;
    while (i < 6) {
        i = i + 1;
        if ((i % 2) == 0) continue;
        sum = sum + i;
    }
    int result = sum
    assertEquals(result, 9)
}