// memory_allocation foo: used 4, spilled 0

#[noinline]
fun foo(int breakIdx) -> int {
    int sum; sum = 0;
    for (int k = 0; ; k = k + 1) {
        if (k == breakIdx) break;
        sum = sum + k;
    }
    return sum
}

fun test_main() {
    int result = foo(5)
    assertEquals(result, 10)
}

// external_calls: 1